# Nixpacks configuration for Railway
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_18", "python3"]

[phases.install]
cmds = ["npm install", "cd server && npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
